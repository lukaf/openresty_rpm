box: lukaf/rpmbuild:latest

build:
    steps:
        - script:
            name: build rpm
            code: |
                cd /mnt/source
                git submodule update --init --recursive
                git submodule foreach git pull origin master
                ./build.sh -b -v 1.7.7.2

